baseURL: "https://haksungjang.github.io/"
title: 장학성 | Haksung

# cSpell:ignore goldmark github hugo readingtime docsy subdir lastmod pygments linenos catmullrom norsk gu

# Language settings
# 콘텐츠 폴더 이름(content/en)과 언어 키(ko)는 무관 — 폴더는 그대로 둔다.
contentDir: content/en
defaultContentLanguage: ko
defaultContentLanguageInSubdir: false
# Useful when translating.
enableMissingTranslationPlaceholders: true

enableRobotsTXT: true

# Will give values to .Lastmod etc.
enableGitInfo: true

# Comment out to enable taxonomies in Docsy
# disableKinds: [taxonomy, taxonomyTerm]

# You can add your own taxonomies
taxonomies:
  tag: tags
  category: categories

# Highlighting config
pygmentsCodeFences: true
pygmentsUseClasses: false
# Use the new Chroma Go highlighter in Hugo.
pygmentsUseClassic: false
# pygmentsOptions: "linenos=table"
# See https://help.farbox.com/pygments.html
pygmentsStyle: tango

# Configure how URLs look like per section.
permalinks:
  blog: /:section/:year/:month/:day/:slug/

# Image processing configuration.
imaging:
  resampleFilter: CatmullRom
  quality: 75
  anchor: smart

# Language configuration
# 콘텐츠가 사실상 전부 한국어이므로 ko로 선언한다.
# html lang=ko, og:locale=ko_KR, RSS <language>ko-KR — 검색엔진·AI가 한국어 사이트로 인식한다.
languages:
  ko:
    languageName: 한국어
    languageCode: ko-KR
    title: Haksung
    params:
      description: Haksung's Homepage 장학성 오픈소스 프로그램 매니저 / SK텔레콤
      time_format_default: 2006-01-02
      time_format_blog: 2006-01-02
  # cSpell:disable
  # 'no':
  #   languageName: Norsk
  #   contentDir: content/no
  #   title: Gulldoks
  #   params:
  #     description: Docsy er operativsystem for skyen
  #     time_format_default: 02.01.2006
  #     time_format_blog: 02.01.2006
  # fa:
  #   languageDirection: rtl
  #   languageName: "فارسی (Persian)"
  #   contentDir: content/fa
  #   title: "اسناد گلدی"
  #   params:
  #     description: "یک نمونه برای پوسته داکسی"
  #     time_format_default: 2006.01.02
  #     time_format_blog: 2006.01.02
  # cSpell:enable

markup:
  goldmark:
    parser:
      attribute:
        block: true
    renderer:
      unsafe: true
  highlight:
    # See a complete list of available styles at https://xyproto.github.io/splash/docs/all.html
    style: tango
    # Uncomment if you want your chosen highlight style used for code blocks without a specified language
    # guessSyntax: true

# Everything below this are Site Params

# markdown·LLMS 출력은 Docsy 내장 템플릿(all.md, index.llms.txt)을 사용한다.
# /llms.txt 와 페이지별 index.md 가 생성되어 AI 크롤러가 마크다운 원문을 수집할 수 있다.
outputs:
  home: [HTML, RSS, markdown, LLMS]
  section: [HTML, print, RSS, markdown]
  page: [HTML, markdown]

# Google Analytics 4. 측정 ID가 설정되면 Docsy가 gtag.js를 자동 삽입한다.
# 프로덕션 빌드(hugo)에서만 동작하고, 로컬(hugo server)에서는 추적되지 않는다.
services:
  googleAnalytics:
    id: G-4QCM5VHF7M
  rss:
    limit: 50

params:
  # 네이버 서치어드바이저·Bing 웹마스터 소유 확인 메타태그 값.
  # 각 서비스에 사이트를 등록하면 받는 content 값을 넣는다.
  # head 출력은 layouts/_partials/hooks/head-end.html 에서 처리한다.
  naver_site_verification: "0f8fd1e446e52bf05a391ca517c84114493b21ce"
  # bing_site_verification: ""

  # Enable Mermaid diagram rendering for fenced ```mermaid code blocks.
  # fontSize/themeVariables는 좁은 폭에서 한글 가독성을 위해 키운 값.
  mermaid:
    enable: true
    theme: default
    config:
      themeVariables:
        fontSize: "18px"
      flowchart:
        useMaxWidth: true
        htmlLabels: true
        curve: basis
      timeline:
        useMaxWidth: true

  taxonomy:
    # set taxonomyCloud = [] to hide taxonomy clouds
    taxonomyCloud: [tags, categories]

    # If used, must have same length as taxonomyCloud
    taxonomyCloudTitle: [Tag Cloud, Categories]

    # set taxonomyPageHeader = [] to hide taxonomies on the page headers
    taxonomyPageHeader: [tags, categories]

  # privacy_policy: https://policies.google.com/privacy

  # First one is picked as the Twitter card image if not set on page.
  # images: [images/project-illustration.png]

  # Menu title if your navbar has a versions selector to access old versions of your site.
  # This menu appears only if you have at least one [params.versions] set.
  version_menu: Releases

  # Flag used in the "version-banner" partial to decide whether to display a
  # banner on every page indicating that this is an archived version of the docs.
  # Set this flag to "true" if you want to display the banner.
  archived_version: false

  # The version number for the version of the docs represented in this doc set.
  # Used in the "version-banner" partial to display a version number for the
  # current doc set.
  version: 0.0

  # A link to latest version of the docs. Used in the "version-banner" partial to
  # point people to the main doc site.
  url_latest_version: https://haksungjang.github.io/

  # Repository configuration (URLs for in-page links to opening issues and suggesting changes)
  github_repo: https://github.com/haksungjang/haksungjang.github.io

  # An optional link to a related project repo. For example, the sibling repository where your product code lives.
  github_project_repo: https://github.com/haksungjang/haksungjang.github.io

  # Specify a value here if your content directory is not in your repo's root directory
  # github_subdir: ""

  # Uncomment this if your GitHub repo does not have "main" as the default branch,
  # or specify a new value if you want to reference another branch in your GitHub links
  github_branch: master

  # Google Custom Search Engine ID. Remove or comment out to disable search.
  gcs_engine_id: 044e5f1b127c4a360

  # Enable Lunr.js offline search
  offlineSearch: false

  # Enable syntax highlighting and copy buttons on code blocks with Prism
  prism_syntax_highlighting: false

  copyright:
    authors: 장학성 | [CC BY 4.0](https://creativecommons.org/licenses/by/4.0) |
    from_year: 2018

  # User interface configuration
  ui:
    # Set to true to disable breadcrumb navigation.
    breadcrumb_disable: false
    # Set to false if you don't want to display a logo (/assets/icons/logo.svg) in the top navbar
    navbar_logo: false
    # Set to true if you don't want the top navbar to be translucent when over a `block/cover`, like on the homepage.
    navbar_translucent_over_cover_disable: false
    # Enable to show the side bar menu in its compact state.
    sidebar_menu_compact: true
    # Set to true to hide the sidebar search box (the top nav search box will still be displayed if search is enabled)
    sidebar_search_disable: false
    sidebar_menu_foldable: true

    # Adds a H2 section titled "Feedback" to the bottom of each doc. The responses are sent to Google Analytics as events.
    # This feature depends on [services.googleAnalytics] and will be disabled if "services.googleAnalytics.id" is not set.
    # If you want this feature, but occasionally need to remove the "Feedback" section from a single page,
    # add "hide_feedback: true" to the page's front matter.
    feedback:
      enable: true
      # The responses that the user sees after clicking "yes" (the page was helpful) or "no" (the page was not helpful).
      'yes': >-
        Glad to hear it! Please <a href="https://github.com/haksungjang/haksungjang.github.io/issues/new">tell us how we can improve</a>.
      'no': >-
        Sorry to hear that. Please <a href="https://github.com/haksungjang/haksungjang.github.io/issues/new">tell us how we can improve</a>.

    # Adds a reading time to the top of each doc.
    # If you want this feature, but occasionally need to remove the Reading time from a single page,
    # add "hide_readingtime: true" to the page's front matter
    readingtime:
      enable: false

  links:
    # End user relevant links. These will show up on left side of footer and in the community page if you have one.
    user:
      - name: User mailing list
        url: "mailto:hakssung@gmail.com"
        icon: fa fa-envelope
        desc: Discussion and help from your fellow users
      - name: Twitter
        url: https://twitter.com/haksungjang
        icon: fab fa-twitter
        desc: Follow us on Twitter to get the latest news!
      - name: Facebook
        url: https://www.facebook.com/haksung.jang
        icon: fab fa-facebook
        desc: Follow me on Facebook!
    # Developer relevant links. These will show up on right side of footer and in the community page if you have one.
    developer:
      - name: GitHub
        url: https://github.com/haksungjang
        icon: fab fa-github
        desc: Development takes place here!
      - name: LinkedIn
        url: https://linkedin.com/in/haksung
        icon: fab fa-linkedin
        desc: Follow me on LinkedIn!
      - name: Developer mailing list
        url: mailto:haksung@sk.com
        icon: fa fa-envelope
        desc: Discuss development issues around the project

module:
  # Uncomment the next line to build and serve using local docsy clone declared in the named Hugo workspace:
  # workspace: docsy.work
  hugoVersion:
    extended: true
    min: 0.110.0
  imports:
    - path: github.com/google/docsy
      disable: false
